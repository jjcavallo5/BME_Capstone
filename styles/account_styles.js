import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  container: {
    height: '100%',
    width: '100%',
    padding: 20,
    display: 'flex',
    alignItems: 'center',
  },
  header: {
    padding: 20,
    display: 'flex',
    alignItems: 'center',
    width: '100%',
    position: 'relative',
  },
  settingsIcon: {
    position: 'absolute',
    top: 10,
    left: 10,
  },
});

export default styles;
