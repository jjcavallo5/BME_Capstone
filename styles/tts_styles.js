import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  container: {
    height: '100%',
    width: '100%',
    display: 'flex',
    alignItems: 'center',
  },
  ttsBar: {
    width: 300,
    height: 50,
    borderRadius: 10,
  },
});

export default styles;
